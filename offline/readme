离线部分：根据提供的语料，创建词典。
	1、去除语料中没用的字符，只留下英文大小写
	2、统计所有单词的字频率-> word frequency
	3、创建词典文件dict.txt,把第二部得到的结果写入文件

source文件夹下存放的是英文语料，可以存放多个文件,运行src/processYuliao下的文件就可以处理文件夹下的所有语料文件
cnsource文件夹下存放的是在一个中文语料，是项目最后加上去的，暂时只能放到file1.txt这个指定的文件里，必须是utf-8编码格式
doc文件夹下的是语料处理完生成的词典，这部分是项目在线部分要用的到，一个是英文词典，一个是中文词典

src/Index文件夹是用来测试建立的索引对不对，index.bat是最后生成的索引格式,可以删掉;
	a 后面的数字表示a这个字符在词典中第几行出现过
